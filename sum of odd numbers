def sum_of_odd_numbers(limit):
    sum = 0
    for i in range(1, limit + 1, 2):
        sum += i
    return sum

limit = int(input("Enter the limit: "))
result = sum_of_odd_numbers(limit)
print("Sum of odd numbers up to {} is: {}".format(limit, result))
